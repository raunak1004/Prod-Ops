project_id = "zaptqsllaxhiwandsexv"

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = [
  "https://684a3574-4021-407a-98f1-51c3b4f072cb.lovableproject.com/**",
  "https://id-preview--684a3574-4021-407a-98f1-51c3b4f072cb.lovable.app/**",
  "https://preview--product-ops-command-center.lovable.app/**",
  "https://product-ops-command-center.lovable.app/**"
]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10

[auth.mfa]
max_enrolled_factors = 10

[auth.mfa.totp]
enroll_enabled = true
verify_enabled = true

[auth.mfa.phone]
enroll_enabled = false
verify_enabled = false

[auth.email]
enable_signup = true
double_confirm_changes = true
enable_confirmations = true
secure_password_change = false
max_frequency = "1m0s"
otp_length = 6
otp_expiry = 86400

[auth.email.template]
# Optional: define custom email templates here (left empty for now)

[auth.sms]
enable_signup = false
